const express = require('express');
    const multer = require('multer');
    const fs = require('fs');
    const path = require('path');
    
    
    const router = express.Router();
   );
    
    // Generate a random filename with a maximum of 5 characters
    
      
      module.exports = router;